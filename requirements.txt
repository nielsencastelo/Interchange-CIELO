# ============================================================
# Interchange AI — Dependências Python
# ============================================================
# Instalar com:
#   pip install -r requirements.txt
#
# Windows: se tiver erro no psycopg, use:
#   pip install -r requirements.txt --ignore-requires-python
# ============================================================

# --- API e servidor ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# --- Dados e ORM ---
pandas>=2.2.0
sqlalchemy>=2.0.30

# --- PostgreSQL (opcional — só necessário com Docker/Postgres)
# No Windows, instale SEPARADAMENTE apenas se usar PostgreSQL:
#   pip install psycopg2-binary
# psycopg[binary]>=3.1.19

# --- Validação e configuração ---
pydantic>=2.8.0
pydantic-settings>=2.4.0
python-dotenv>=1.0.1

# --- Leitura de PDF ---
pdfplumber>=0.11.4
pypdf>=5.0.1

# --- HTTP / LLM ---
httpx>=0.27.0
anthropic>=0.30.0

# --- Dashboard ---
streamlit>=1.37.0
plotly>=5.22.0

# --- Relatório ---
jinja2>=3.1.4

# --- Testes ---
pytest>=8.0.0
pytest-cov>=5.0.0

# --- Utilitários ---
rich>=13.7.0
click>=8.1.7
matplotlib